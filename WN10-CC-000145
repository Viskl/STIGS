<#
.SYNOPSIS
    This PowerShell script ensures that users are prompted for a password on resume from sleep (on battery).
.NOTES
    Author          : Sebastian Chrzanowski
    LinkedIn        : www.linkedin.com/in/sebastian-chrzanowski-75a8a41a4/
    GitHub          : github.com/Viskl
    Date Created    : 2025-03-11
    Last Modified   : 2025-03-11
    Version         : 1.0
    CVEs            : N/A
    Plugin IDs      : N/A
    STIG-ID         : WN10-CC-000145
.TESTED ON
    Date(s) Tested  : 
    Tested By       : 
    Systems Tested  : 
    PowerShell Ver. : 
.USAGE
    Run the script with administrator privileges.
    Example syntax:
    PS C:\> .\remediation_WN10-CC-000145.ps1
#>

$registryPath = "HKLM:\SOFTWARE\Policies\Microsoft\Power\PowerSettings\0e796bdb-100d-47d6-a2d5-f7d2daa51f51"
$valueName    = "DCSettingIndex"
$valueData    = 1

# Create registry path if it does not exist
if (-not (Test-Path $registryPath)) {
    New-Item -Path $registryPath -Force | Out-Null
    Write-Host "Registry path created: $registryPath"
}

# Set the registry value
Set-ItemProperty -Path $registryPath -Name $valueName -Value $valueData -Type DWord
Write-Host "Registry value '$valueName' set to '$valueData' at '$registryPath'"

# Verify the setting was applied
$appliedValue = Get-ItemProperty -Path $registryPath -Name $valueName
Write-Host "Verification - Current value: $($appliedValue.$valueName)"
